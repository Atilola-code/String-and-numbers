let num_type = x = 12 ? "Even" : "Odd"
console.log(num_type)

let sign = -5
let y = sign > 0 ? "Positive" : "Negative"
console.log(y)

let score = 75
let grade = score >= 50 ? "Pass" : "Fail"
console.log(grade)

let length = "Hello World".length
let str = length > 10 ? "Long" : "Short"
console.log(str)

let p = 10
let q = 10
let is_equal = p == q ? "Yes" : "No"
console.log(is_equal)
